<header>
    <nav class="nav">
      <h1 class="big-head">{ Stephen: Knutter }</h1>
    </nav>
    <hr />
    <div class="row head-menu">
      <a href="/" ng-class="{active: $state.current.data.navSelect == 'home'}">Home</a>
      <a href="#" ng-class="{active: $state.current.data.navSelect == 'archive'}">Archive</a>
    </div>
</header>
